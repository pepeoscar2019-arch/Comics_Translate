# Ambiente principale: pipeline, GUI Tkinter, web app.
#   python -m venv venv
#   venv\Scripts\python.exe -m pip install -r requirements.txt
#
# Nessuna dipendenza da servizi cloud: OCR e traduzione girano su
# llama-server (llama.cpp), installato a parte (vedi README).
Flask>=3.1
numpy>=2.0
opencv-python-headless>=4.10
pillow>=11.0
pyphen>=0.17
PyYAML>=6.0
requests>=2.32
colorama>=0.4.6
